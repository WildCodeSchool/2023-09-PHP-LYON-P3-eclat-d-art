{% extends 'base.html.twig' %}

{% block title %}Admin Dashboard{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


<div id="card-area-categories">
    <div class="wrapper">
        <h2>User Count</h2>
        <p>There are {{ userCount }} registered users.</p>
        <div class="table-area">
            <h2>Œuvres</h2>
                <table>
                    <thead>
                    <tr>
                        <th>titre</th>
                        <th>description</th>
                    </tr>
                    </thead>
                    {% for artwork in artworks %}

                    <tbody>
                    <tr>
                        <td>{{artwork.title }}</td>
                        <td>{{ artwork.description }}</td>
                        <td><a class="btn btn-primary artwork-descovery" href="{{ path('app_artwork_edit', {'id': artwork.id}) }}" aria-label="Editer l\'oeuvre">Editer</a>
                            {{ include('artwork/_delete_form.html.twig') }}</td>
                    </tr>
                    </tbody>
                    {% endfor %}
                </table>

        </div>
    </div>
</div>
<div id="card-area-categories">
    <div class="wrapper">
        <div class="table-area">
            <h2>Artistes</h2>
            <table>
                    <thead>
                    <tr>
                        <th>Name</th>
                        <th>description</th>
                        <th>email</th>
                    </tr>
                    </thead>
                {% for user in users %}
                <tbody>
                    <tr>
                        <td>{{user.name }}</td>
                        <td>{{ user.description }}</td>
                        <td>{{ user.email }}</td>
                        <td><a class="btn btn-primary artwork-descovery" href="{{ path('app_user_edit', {'id': user.id}) }}" aria-label="Editer l\'oeuvre">Editer</a>
                            {{ include('user/_delete_form.html.twig') }}</td>
                    </tr>
                    </tbody>
            {% endfor %}
            </table>
        </div>
    </div>
</div>
{% endblock %}
