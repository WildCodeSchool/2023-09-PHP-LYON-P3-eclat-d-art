<form method="post" action="{{ path('app_user_delete', {'id': artist.id}) }}" onsubmit="return confirm('Êtes-vous sûr de votre choix ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artist.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
