<nav class="navbar navbar-expand-lg navbar-light line ">
	<div class="container-fluid">
		<a class="navbar-brand" aria-label="Le logo de notre site web qui vous renvoie à notre page d'accueil" href="/">
			<img src=" {{ asset('build/images/logo-eclat.png')}} " alt="" class="logo_nav">
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="link" aria-label="Cliquez ici pour voir tous nos artistes" href="/artiste">Artistes</a>
				</li>
				<li class="nav-item">
					<a class="link" aria-label="Cliquez ici pour voir toutes nos categories" href="/#card-area-categories">Catégories</a>
				</li>
				<li class="nav-item">
					<a class="link" aria-label="Cliquez ici pour voir toutes les oeuvres d'art" href="/artwork/">Oeuvres</a>
				</li>
			</ul>
			{% if is_granted("ROLE_USER") %}
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a href="{{ path('app_artwork_new') }}">Ajouter une oeuvre</a>
					</li>
				</ul>
			{% endif %}
			{% if is_granted("ROLE_ADMIN") %}
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a href="{{ path('admin') }}">Admin dashboard</a>
					</li>
				</ul>
			{% endif %}
			<div>
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						{% if app.user %}
                            <section class="Username">
                                <a href="{{ path('app_user_show', {'id': app.user.id}) }}">{{ app.user.name }} / </a>
							<a href="{{ path('app_logout') }}">Se déconnecter</a>
                            </section>
						{% else %}
							<ul class="register">
								<li class="nav-item">
									<a href="{{ path('register') }}">Inscription</a>&nbsp;<a href="{{ path('login') }}">/ Connexion</a>
								</li>
							</ul>
						{% endif %}
					</li>
				</ul>
			</div>
		</div>
	</div>
</nav>
