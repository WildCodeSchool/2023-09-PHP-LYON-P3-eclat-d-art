<nav class="navbar navbar-expand-lg navbar-light line ">
    <div class="container-fluid">
        <a class="navbar-brand" aria-label="Le logo de notre site web qui vous renvoie à notre page d'accueil" href="/">
            <img src="{{ asset('build/images/logo-eclat.png')}}" alt="" class="logo_nav">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="link" aria-label="Cliquez ici pour voir tous nos artistes" href="{{ path('app_user_index') }}">Artistes</a>
                </li>
                <li class="nav-item">
                    <a class="link" aria-label="Cliquez ici pour voir toutes nos categories" href="/#card-area-categories">Catégories</a>
                </li>
                <li class="nav-item">
                    <a class="link" aria-label="Cliquez ici pour voir toutes les oeuvres d'art" href="{{ path('app_artwork_index') }}">Oeuvres</a>
                </li>

                {% if is_granted("ROLE_USER") %}

                    <li class="nav-item">
                        <a class="link" href="{{ path('app_artwork_new') }}">Ajouter une oeuvre</a>
                    </li>

                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}

                    <li class="nav-item">
                        <a class="link" href="{{ path('admin') }}">Admin dashboard</a>
                    </li>

                {% endif %}

                {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.name }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ path('app_user_show', {'id': app.user.id}) }}">Mon profil</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Me déconnecter</a>
                        </div>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="link" href="{{ path('register') }}">Inscription / </a>&nbsp;<a href="{{ path('login') }}"><i class="fa-solid fa-user" style="color: #7b652d;"></i></a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
