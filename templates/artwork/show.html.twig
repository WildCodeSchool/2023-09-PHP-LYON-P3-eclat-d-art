{% extends 'base.html.twig' %}

{% block title %}Artwork{% endblock %}

{% block body %}
    
<div class="box-area">
    <div class="box-artwork">
        <img class="image" src=" {{ artwork.imageCover }} " alt="image de {{ artwork.title }}" >
    </div>

    <div class="box-right">
        <div class="box-describ">
                <h1>{{ artwork.title }}</h1>
            
            <tr>
                <h6>Créer le : </h6><td>{{ artwork.createdAt ? artwork.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                
            </tr>
            <br>
            <tr>
                <h6>Dimension: </h6>
                <td>{{ artwork.height }} cm X  {{ artwork.weight }} cm</td>
            </tr>
            <br>
            <tr>
                <h6>Technique : </h6>
                <td>{{ artwork.technique }}</td>
            </tr>
            <br>
            <tr>
                <h6>Description : </h6>
                <td>{{ artwork.description }}</td>
            </tr>
        </div>
    
        
        <div class="mini-slides">
            <div class="slide slide-1">
                <img src="{{ asset('build/images/caravage.webp') }}" alt="First slide">
            </div>
            <div class="slide slide-2">
                <img src="{{ asset('build/images/greek.webp') }}" alt="Second slide">
            </div>
            <div class="slide slide-3">
                <img src="{{ asset('build/images/photo.webp') }}" alt="Third slide">
            </div>
            </div>
        </div>
    </div>
</div>

    <a href="{{ path('app_artwork_index') }}">back to list</a>

    <a href="{{ path('app_artwork_edit', {'id': artwork.id}) }}">edit</a>

    {{ include('artwork/_delete_form.html.twig') }}
{% endblock %}
