{% extends 'base.html.twig' %}

{% block body %}

    <h2>Oeuvres : {{ category.name }}</h2>

    <div class="gallery-container">
    {% for artwork in artworks %}
        <div class="gallery">
            <img class=" image-artwork" alt="{{ artwork.description }}" src="{{ artwork.imageCover }}"> 
                <p>{{ artwork.title }}</p>
                    <p>{{ artwork.description }}</p>
                         <a class="btn btn-secondary form-button form-link" href="{{ path('app_artwork_show', {'id': artwork.id}) }} " role="button">Découvrir</a>
        </div>
    {% else %}
        <p>no records found</p>
    {% endfor %}
    </div>
    <a href="{{ path('app_artwork_new') }}">Create new</a>
{% endblock %}
