liip_imagine:
    webp:
        generate: true
    driver: "gd"
    resolvers:
        default:
            web_path: ~
    filter_sets:
        cache: ~
        toto:
            format: webp
            quality: 100
            filters:
                thumbnail: { size: [1000, 1000], mode: outbound }